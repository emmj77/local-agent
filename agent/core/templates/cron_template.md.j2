---
agent: "{{ agent }}"
project: "{{ project }}"
tags: [{% for tag in tags %}"{{ tag }}"{% if not loop.last %}, {% endif %}{% endfor %}]
timestamp: "{{ timestamp }}"
activation: "{{ activation }}"
status: "pending"
---

{{ prompt }}